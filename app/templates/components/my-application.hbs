<body id="page-top">
<nav class="navbar navbar-expand-lg navbar-light navbar-fixed-top" id="mainNav">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="nav navbar-nav ml-auto">
        {{#if isAuthenticated}}
        <li class="nav-item">
          <a class="nav-link js-scroll-trigger">{{#link-to "users"}}Friends{{/link-to}}</a>
          </li>
          <li class="nav-item">
            <a class="nav-link js-scroll-trigger">{{#link-to 'events'}}View Events{{/link-to}}</a>
            </li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li class="nav-item">
          <a class="nav-link js-scroll-trigger">{{#link-to "change-password"}}Change Password{{/link-to}}</a>
          </li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li class="nav-item">
          <a class="nav-link js-scroll-trigger">{{#link-to "sign-up"}}Sign Up{{/link-to}}</a>
          </li>
          <li class="nav-item">
            <a class="nav-link js-scroll-trigger">{{#link-to "sign-in"}}Sign In{{/link-to}}</a>
            </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>



{{!-- <section class='welcome'>
<h1>POOL</h1>
<p>Rides for your kids from your circle of friends.</p>
</section> --}}
<section class='events-area'>
{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="content">
  {{outlet}}
</div>

</section>
</body>
